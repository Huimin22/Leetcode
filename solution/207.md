## 1. Problem

207. Course Schedule

## 2. Summary

Topograph sort

## 3. Key Points

* Build graph (adjacency list) and in-degree array
* Queue = courses with in-degree 0
* BFS: pop course, reduce in-degree of neighbors, enqueue neighbors with in-degree 0
* Count visited courses
* If visited == numCourses → True, else → False (cycle)

## 4. Code Implementation:

```
class Solution:
    def canFinish(self, numCourses: int, prerequisites: List[List[int]]) -> bool:
        in_degree = [0]*numCourses
        graph = defaultdict(list)

        for course, pre in  prerequisites:
            graph[pre].append(course)
            in_degree[course] += 1

        q = deque()
        for i in range(numCourses):
            if in_degree[i] == 0:
                q.append(i)
            
        visited = 0
        
        while q:
            cur = q.popleft()
            visited += 1
            for nei in graph[cur]:
                in_degree[nei] -= 1
                if in_degree[nei] == 0:
                    q.append(nei)
        
        return  visited == numCourses   
```

## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note

* Decreased `cur`’s in-degree instead of `nei`
* Checked `cur`’s in-degree instead of `nei` for queue
* Appended in-degree values to queue instead of course indices

**Second**

* Confused adjacency list: mixed up prerequisites and next courses.
* Forgot to build in-degree table.
* Unclear on what to add to initial queue.

**Third**

* Not sure how to build graphlist
* Don't know how to use bfs.
* Confused with different para'name. Very easily be confused with different para. Shoule name more specific.

**Fourth**

1. Enqueued in_degree values instead of course indices.
2. Decreased in_degree of wrong node (was correct in later version).
3. Confused node IDs vs in_degree values during BFS.
4. Didn’t clearly separate graph keys (course) and in_degree values.

Misunderstanding:

1. Confused in-degree values with course IDs
   - In-degree array: index = course ID, value = number of remaining prerequisites
   - The array element is a count, not a course identifier

2. Queue should store course IDs, not in-degree values
   - Only nodes (course IDs) go into the queue
   - In-degree array is just used to check if a course is ready to take (in-degree == 0)

3. When popping a course from the queue:
   - Reduce in-degree of its dependent courses
   - Enqueue dependent courses whose in-degree becomes 0

4. Naming tip to avoid confusion:
   - `cur_course` or `course_id` for nodes
   - `in_degree[course_id]` for counts
   - Keep node vs. status clearly separated in variable names

Mental Model:

- Queue = courses ready to take
- In-degree table = counts of remaining prerequisites
- Index = course ID, value = number of prerequisites left
- Never enqueue in-degree values; only enqueue course IDs
