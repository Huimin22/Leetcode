## 1. Problem

42. Trapping Rain Water(hard)
    
## 2. Summary

water[i] = min(left_max, right_max) - height[i]

Water level at i is limited by the shorter wall between left_max and right_max.

## 3. Key Points

1. Build left_max array
   left_max[i] = max(left_max[i-1], height[i])

2. Build right_max array
   right_max[i] = max(right_max[i+1], height[i])

3. Compute water
   water += max(min(left_max[i], right_max[i]) - height[i], 0)


## 4. Code Implementation:

```
class Solution:
    def trap(self, height: List[int]) -> int:
        n = len(height)
        if n == 0:
            return 0
        
        max_left = [0] * n
        max_left[0] = height[0]
        for i in range(1, n):
            max_left[i] = max(max_left[i-1], height[i])
        
        max_right = [0] * n
        max_right[-1] = height[-1]
        for i in range(n-2, -1, -1):
            max_right[i] = max(max_right[i+1], height[i])
        
        water = 0
        for i in range(n):
            water += max(min(max_left[i], max_right[i]) - height[i], 0)
        
        return water   
```

## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note

**Second**

1. Overwrote array:
   max_right = height[-1]
   Should be: max_right[-1] = height[-1]

2. Wrong loop range:
   for i in range(0) → loop never runs
   Should be: for i in range(n)

3. Tuple instead of max():
   (min(...) - height[i], 0) → tuple
   Should be: max(min(...) - height[i], 0)

4. Forgot why max(...,0):
   min(max_left, max_right) - height[i] can be negative when column is higher than water level. Water cannot be negative.

