## 1. Problem

706. Design HashMap
    
## 2. Summary



## 3. Key Points




## 4. Code Implementation:

```
class MyHashMap:

    def __init__(self):
        self.size = 769
        self.table = [[] for _ in range(self.size)]

    def put(self, key: int, value: int) -> None:
        index = key % self.size
        bucket = self.table[index]

        for pair in bucket:
            if pair[0] == key:
                pair[1] = value
                return

        bucket.append([key, value])

    def get(self, key: int) -> int:
        index = key % self.size
        bucket = self.table[index]

        for k, v in bucket:
            if k == key:
                return v

        return -1

    def remove(self, key: int) -> None:
        index = key % self.size
        bucket = self.table[index]

        for i, (k, v) in enumerate(bucket):
            if k == key:
                bucket.pop(i)
                return


# Your MyHashMap object will be instantiated and called as such:
# obj = MyHashMap()
# obj.put(key,value)
# param_2 = obj.get(key)
# obj.remove(key)   
```

## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note

**Second**

```text
1. Used `=` instead of `==` for comparison.  
2. Typo: `ley` instead of `key`.  
3. Treated list of pairs as dict (`bucket[k]`), misunderstanding structure.  
4. Didn’t update value correctly; must modify `pair[1]` or `bucket[i][1]`.  
5. Confused `for pair in bucket` with `for k, v in bucket` unpacking.  
6. Unfamiliar with `enumerate` to get index when modifying list elements.
```

