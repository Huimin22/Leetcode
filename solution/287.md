## 1. Problem

287. Find the Duplicate Number
    
## 2. Summary

Node = index, Node.next = nums[index]

Edge: i -> nums[i]


Why a duplicate forms a cycle:

When two indices point to the same value:

```
nums[a] = x
nums[b] = x
```

It becomes:

```
a -> x
b -> x
```

And since `x` points to another node to b, this eventually forms a cycle.

```
a -> x ...b -> x

```

## 3. Key Points

* Key idea: first meeting encodes cycle start; second phase → entry.

* Treat array as **linked list**: index → value → next index

* **Detect cycle (Floyd’s Tortoise & Hare)**

  * slow = nums[0], fast = nums[0]
  * while True:

    * slow = nums[slow]
    * fast = nums[nums[fast]]
    * if slow == fast: break

* **Find cycle entry (duplicate)**

  * slow2 = nums[0]
  * while slow != slow2:

    * slow = nums[slow]
    * slow2 = nums[slow2]
  * return slow
 
* Treat array as linked list: index → value → next index
* Use Floyd’s cycle detection (slow=1 step, fast=2 steps) to find first meeting
* Second phase: slow from meeting, slow2 from head, 1 step each → meet at duplicate
* Time O(n), space O(1)
* First meeting encodes cycle start; second phase finds entry


* **Index as node**: each index uniquely identifies a position; a duplicate number → two different indices pointing to the same index → forms a cycle.
* **Value as pointer, index as node** → abstract the array as a linked list.

## 4. Code Implementation:


```
class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        slow = nums[0]
        fast = nums[0]
        while True:
            slow = nums[slow]
            fast = nums[nums[fast]]
            if slow == fast:
                break
        
        slow2 = nums[0]
        while slow != slow2:
            slow2 = nums[slow2]
            slow = nums[slow]
        return slow   


```

## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note

**Second**

Pass.

**Third**

Pass.

**Fourth**

Pass.
