## 1. Problem

322. Coin Change
    
## 2. Summary

```python
dp = [inf]*(amount+1)
dp[0] = 0
for coin in coins:
    for i in range(coin, amount+1):
        dp[i] = min(dp[i], dp[i-coin]+1)
return dp[amount] if dp[amount] != inf else -1
```

## 3. Key Points

* `dp[i]` = min coins to make amount i
* Loop coins → loop i ≥ coin → update dp[i]
* Base case: dp[0] = 0
* If dp[amount] still inf → impossible, return -1

## 4. Code Implementation:

```
class Solution:
    def coinChange(self, coins: List[int], amount: int) -> int:
        dp = [float('inf')] * (amount+1)
        dp[0] = 0

        for coin in coins:
            for i in range(coin, amount+1):
                dp[i] = min(dp[i], dp[i-coin] + 1)

        if dp[amount] == float('inf'):
            return -1
        else:
            return dp[amount]
   

```

## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note

**Mistake**

* `for i from range(...)` → syntax wrong, should be `for i in range(...)`
* `range(coin, amount)` → misses `amount`, should be `range(coin, amount+1)`
* Missing base case `dp[0] = 0`
* Forget to return `-1` if amount cannot be made

**Second**

* Wrong state transition index: used `amount - coin` instead of `i - coin`.
* Did not follow DP meaning: `dp[i]` depends on `dp[i - coin]`, not final amount.
* Mixed up loop variable (`i`) with target (`amount`).
* Broke subproblem logic by not using current state `i`.

**Third**

1. Used max instead of min
   - This is a minimum coins problem (minimization DP)

2. Didn’t check unreachable state
   - Need to return -1 if dp[amount] == inf

3. Didn’t clearly identify problem type:
   - Complete knapsack (unlimited coins)
   - Transition: dp[i] = min(dp[i], dp[i-coin] + 1)

**Fourth**

Pass.
