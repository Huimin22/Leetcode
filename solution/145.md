## 1. Problem

145. Binary Tree Postorder Traversal
    
## 2. Summary

```python
# LC145 Postorder DFS Key Points

# Traversal order: Left -> Right -> Root

def dfs(node):
    if not node:
        return
    dfs(node.left)    # recurse left
    dfs(node.right)   # recurse right
    res.append(node.val)  # process root

# usage
res = []
dfs(root)
return res
```

## 3. Key Points

```python
# LC145 Postorder DFS Key Points

# Order: Left -> Right -> Root
# Base case: if node is None, return
# Recurse left, recurse right, process root
# Define dfs, call dfs(root), return res
```

## 4. Code Implementation:

```
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def postorderTraversal(self, root: Optional[TreeNode]) -> List[int]:
        res = []
        def dfs(node):
            if not node:
                return
            dfs(node.left)
            dfs(node.right)
            res.append(node.val)
        dfs(root)
        return res   
```

## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note

**Second**

```python
#  Mistakes

# 1. Defined dfs but forgot to call dfs(root)
# 2. Returned res before running dfs
# 3. Confused order: must append after left and right
# 4. Missed base case check for None
```

**Third**

Pass.

**Fourth**

Pass.

**Fifth**

Pass.

