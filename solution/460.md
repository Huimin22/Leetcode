## 1. Problem

460. LFU Cache
    
## 2. Summary

`get` method: 

if key exists, return value and call `_update_freq`; if not, return -1.

`_update_freq` method:

Delete the old `freq_table[freq][key]` / `freq_table[freq]`, increase `freq` by 1, move the key from the old freq bucket to the new freq bucket, and update `key_table` and `min_freq`.

`put` method: 

self.capacity == 0; update key if exists and call `_update_freq`; if new key, evict oldest if full and update `size`,  insert with freq=1, update `key_table`, `freq_table`, `min_freq`, and `size`.

## 3. Key Points

Data Structures

* key_table: key → (value, freq)
* freq_table: freq → OrderedDict(keys)  (maintain LRU inside same freq)
* 0: (val, none) 
* freq_table = {
    1: {k1: None, k2: None},
    2: {k3: None}
} we don't care about the fre val here.
* min_freq: smallest frequency in cache

_update_freq(key) key in table

1. get (value, freq) from key_table
2. remove key from freq_table[freq]
3. if this freq list becomes empty:
   * delete it
   * if freq == min_freq → min_freq += 1
4. freq += 1
5. add key to freq_table[freq] (at end)
6. update key_table[key] = (value, freq)

get(key)

* if key not exist → return -1
* update frequency
* return value

put(key, value)

1. if capacity == 0 → return

2. if key exists

   * update value
   * update frequency

3. if key not exists

   * if size == capacity

     * evict from freq_table[min_freq]
     * pop oldest key (LRU within that freq)
   * insert new key with freq = 1
   * min_freq = 1
     
## 4. Code Implementation:

```
from collections import defaultdict, OrderedDict

class LFUCache:

    def __init__(self, capacity: int):
        self.capacity = capacity
        self.size = 0
        self.min_freq = 0
        self.key_table = {}
        self.freq_table = defaultdict(OrderedDict)

    def _update_freq(self, key):
        value, freq = self.key_table[key]

        del self.freq_table[freq][key]
        if not self.freq_table[freq]:
            del self.freq_table[freq]
            if freq == self.min_freq:
                self.min_freq += 1

        freq += 1
        self.freq_table[freq][key] = None
        self.key_table[key] = (value, freq)
        
    def get(self, key: int) -> int:
        if key not in self.key_table:
            return -1
        self._update_freq(key)
        return self.key_table[key][0]

    def put(self, key: int, value: int) -> None:
        if self.capacity == 0:
            return

        if key in self.key_table:
            self.key_table[key] = (value, self.key_table[key][1])
            self._update_freq(key)

        else:
            if self.size >= self.capacity:
                oldest_key, _ = self.freq_table[self.min_freq].popitem(last=False)
                del self.key_table[ oldest_key]
                self.size -= 1
            self.key_table[key] = (value, 1)
            self.freq_table[1][key] = None
            self.min_freq = 1
            self.size += 1


# Your LFUCache object will be instantiated and called as such:
# obj = LFUCache(capacity)
# param_1 = obj.get(key)
# obj.put(key,value)  


```

## 5. Complexity Analysis
    Time：O(1)
        
    Space: O(1)
    
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note

**Second**

* Unfamiliar with **nested data structures**: `dict → OrderedDict` (freq_table design).
* Weak understanding of **OrderedDict usage**: insertion, maintaining LRU order, `popitem(last=False)`.
* Confusion about **using dict as a set** (`freq_table[1][key] = None`).
* Not comfortable with **defaultdict initialization** (`defaultdict(OrderedDict)`).
* Difficulty understanding **why freq_table stores only keys while key_table stores value/freq**.
* Limited familiarity with **LFU bucket structure (freq → ordered keys)**.

**Third**

**`put` method:**

* **If key exists:** directly update `key_table` and update `freq_table`.
* **If key does not exist:** first check if `size` exceeds capacity.

* **If exceeded:** remove the oldest key from `freq_table` at the current `min_freq`, also delete it from `key_table`, and update `size`.
  * Add the new key with `freq = 1` to both tables, and update `min_freq` and `size`.
 
* `popitems` should be `popitem`
* `self.size += 1` should not increase when key exists
* updating existing key in `put` did not update `freq_table` (before adding `_update_freq`)

**Fourth**

put method: check if self.capacity == 0:

Check `capacity == 0`:

* prevent deleting from an empty `freq_table`
* avoid `popitem()` errors
* ensure LFU cache works correctly for the `capacity = 0` edge case

**Fifth**

put method: check if self.capacity == 0:

Check `capacity == 0`:

**Sixth**

val, freq = key_table[key]   # don’t swap order
tuples are immutable → rebuild instead of modifying
min_freq only updates when the bucket becomes empty
