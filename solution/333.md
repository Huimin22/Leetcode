## 1. Problem

333  Largest BST Subtree
     
Given a binary tree, find the largest subtree which is a Binary Search Tree (BST), where largest means subtree with largest number of nodes in it.

Note:
A subtree must include all of its descendants.

Example:

Input: [10,5,15,1,8,null,7]

```
   10 
   / \ 
  5  15 
 / \   \ 
1   8   7

```

Output: 3

Explanation: The Largest BST Subtree in this case is the highlighted one.
             The return value is the subtree's size, which is 3.
             
Follow up:

Can you figure out ways to solve it with O(n) time complexity?

Hint:

You can recursively use algorithm similar to 98. Validate Binary Search Tree at each node of the tree, which will result in O(nlogn) time complexity.    
## 2. Summary



## 3. Key Points




## 4. Code Implementation:


```
class Solution:
    def largestBSTSubtree(self, root):
        self.ans = 0

        def dfs(node):
            if not node:
                return True, 0, float('inf'), float('-inf')

            left_is, left_size, left_min, left_max = dfs(node.left)
            right_is, right_size, right_min, right_max = dfs(node.right)

            if left_is and right_is and left_max < node.val < right_min:
                size = left_size + right_size + 1
                self.ans = max(self.ans, size)
                return True, size, min(left_min, node.val), max(right_max, node.val)

            return False, 0, 0, 0

        dfs(root)
        return self.ans   


```

## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note
