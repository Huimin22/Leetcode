## 1. Problem

739. Daily Temperatures
    
## 2. Summary

* Use a stack to store **indices** of days waiting for a warmer temperature
* Traverse temperatures left to right
* While stack not empty and `current_temp > temperatures[stack[-1]]`:
* Pop index, set `answer[index] = current_index - index`
* Push current index onto stack
* Remaining indices in stack → answer = 0

## 3. Key Points

* Stack stores indices of unresolved days
* Traverse temperatures left to right
* While stack and `current > temperatures[stack[-1]]`: pop and update answer
* Push current index to stack
* Remaining stack → answer = 0

## 4. Code Implementation:

```
class Solution:
    def dailyTemperatures(self, temperatures: List[int]) -> List[int]:
        n = len(temperatures)
        answer = [0] * n
        stack = []

        for i, temp in enumerate(temperatures):
            while stack and temp > temperatures[stack[-1]]:
                prev_index = stack.pop()
                answer[prev_index] = i - prev_index
            stack.append(i)

        return answer   

```

## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note

**Second**

* Compared `temperatures[i]` with `stack[-1]` instead of `temperatures[stack[-1]]`
* Forgot stack stores **indices**, not temperatures
* Mixed index vs value when doing comparison logic

**Third**

* Compared `temperatures[i]` with `stack[-1]` instead of `temperatures[stack[-1]]`
* Forgot stack stores **indices**, not temperatures
* Mixed index vs value when doing comparison logic

**Fourth**

* Assigned to index not initialized → **res[prev] used before res has size n**
* Forgot pre-allocation → must **init res = [0] * n**
* Misunderstood list assignment → Python list **cannot assign beyond current length**
* Logic ok, structure wrong → **need fixed-size result for index-based updates**

* Used **if instead of while** → only popped **one** index, missed multiple smaller temps
* Monotonic stack invariant broken → stack should be **strictly decreasing**, needs **while loop**
* Leads to incomplete updates → some indices **never get resolved**
* Pattern mistake → **“pop all smaller before push”**, not just one

