## 1. Problem

257. Binary Tree Paths
    
## 2. Summary

DFS(root, path):

if not root:
return

if leaf:

add path + root to res
return

DFS(left, path + root + "->")
DFS(right, path + root + "->")

## 3. Key Points

root-to-leaf path tracking via DFS
carry path string or list
at leaf, finalize path into result

## 4. Code Implementation:


```
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def binaryTreePaths(self, root: Optional[TreeNode]) -> List[str]:
        res = []

        def dfs(node, path):
            if not node:
                return

            if not node.left and not node.right:
                res.append(path + str(node.val))
                return

            if node.left:
                dfs(node.left, path + str(node.val) + "->")
            if node.right:
                dfs(node.right, path + str(node.val) + "->")

        dfs(root, "")
        return res   
```

## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note

**Second**

Pass.

**Third**

* **Result Persistence**: Using `return` after `res.append()` in a recursive function is correct for a leaf node, but the logic fails if the current `path` string was not properly passed down from the parent.

* **Path Accumulation**: `path + str(node.val)` only creates the correct string at the leaf if the `path` variable already contains all preceding nodes and arrows (e.g., `"1->2->"`); otherwise, the output format will be incomplete.

* **Root Initiation**: `dfs(root, "")` is correct for the first call, but it requires the internal logic to handle the "->" separator conditionally to avoid trailing arrows or missing segments.
res.append(path+str(node.val))

**Fourth**

Forgot to add the last node val.

res.append(path + str(node.val))

