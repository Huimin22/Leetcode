## 1. Problem

    
## 2. Idea
Two pointer


## 3. Key Points
Normalize the string first, then use two pointers to compare from both ends, returning false on any mismatch and true if the loop finishes.



## 4. Code Implementation:


```
class Solution:
    def isPalindrome(self, s: str) -> bool:
        if len(s) == 0 or len(s) == 1:
            return True
        pro_s = []
        for c in s:
            if c.isalnum():
                pro_s.append(c.lower())
    //new_str = ''.join(c.lower() for c in s if c.isalnum())
        new_str = ''.join(pro_s)
        print(new_str)
        i, j = 0, len(new_str) - 1
        while i < j:    
            if new_str[i] == new_str[j]:
                i += 1
                j -= 1
                continue
            
            else:
                return False
        return True
```

better not use continue, use less loop

V2:
```
code
 
class Solution:
    def isPalindrome(self, s: str) -> bool:
        i, j = 0, len(s) - 1

        while i < j:
            while i < j and not s[i].isalnum():
                i += 1
            while i < j and not s[j].isalnum():
                j -= 1

            if s[i].lower() != s[j].lower():
                return False

            i += 1
            j -= 1

        return True

```


## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

**Use two pointers on the original string, skip non-alphanumeric characters on the fly, compare lowercase characters, and fail early on any mismatch.**

## 8. Note

I focused too much on the loop logic and missed that finishing the loop without returning false already means the result should be true.

**Second Time:**

Pointer movement with inner while loops.

Character validation using .isalnum(), and normalization with .lower(). 

I learned that True should be returned after the main loop ends. And tyied to filter the non-alnum on the fly.
