## 1. Problem

98. Validate Binary Search Tree
    
## 2. Summary

* Empty node → True
* Define helper function;
* Check node in range, return False if not;
* Recursively check left and right subtrees.


## 3. Key Points

```python
def isValidBST(root):
    def dfs(node, lo, hi):
        if not node: return True
        if not (lo < node.val < hi): return False
        return dfs(node.left, lo, node.val) and dfs(node.right, node.val, hi)
    return dfs(root, float('-inf'), float('inf'))
```

* Node must satisfy `min < val < max`
* Left subtree: `max = node.val`
* Right subtree: `min = node.val`
* Empty node → True
* Recurse both subtrees

## 4. Code Implementation:

```
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def isValidBST(self, root: Optional[TreeNode]) -> bool:
        def helper(node, low, high):
            if not node:
                return True
            if not (low < node.val < high):
                return False
            return helper(node.left, low, node.val) and helper(node.right, node.val, high)
        return helper(root, float('-inf'), float('inf'))
```

## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note

**Second**

1. Used `float('int')` instead of `float('-inf')` / `float('inf')`.
2. Swapped `low` and `high` in initial call.
3. Wrote `low < root.val < root.val` instead of `low < root.val < high`.
4. Forgot helper function is needed to pass `low`/`high` in recursion.

**`isTree(root, low, high)` function:**

> Check whether the subtree rooted at `root` satisfies **BST** properties, ensuring all node values are within the range `(low, high)`.
>
> * Return `True` if the subtree satisfies BST conditions
> * Return `False` if the subtree does not satisfy BST conditions

**Third**

Didn't check the null node in the helper function.


**Fourth**

* `self` is needed when calling **another method of the class**, e.g., `self.other_method()`
* **Not needed** for a nested function defined inside a method, like `helper()` here → just call `helper(...)`
* `self` is **always the first argument of class methods**, but local functions capture variables from outer scope automatically.

So here: `helper(root, float('-inf'), float('inf'))` ✅ correct, no `self` needed.

Pass.

**Fifth**

Pass.

**Sixth**

Mismatch root and node.

Pass.

