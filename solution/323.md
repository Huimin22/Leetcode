## 1. Problem

323. Number of Connected Components in an Undirected Graph (locked)

Given n nodes labeled from 0 to n - 1 and a list of undirected edges (each edge is a pair of nodes), write a function to find the number of connected components in an undirected graph.

Example 1:

     0          3

     |          |

     1 — 2      4

Given n = 5 and edges = [[0, 1], [1, 2], [3, 4]], return 2.

Example 2:

     0       4

     |       |

     1 — 2 — 3

Given n = 5 and edges = [[0, 1], [1, 2], [2, 3], [3, 4]], return 1.

 Note:

You can assume that no duplicate edges will appear in edges. Since all edges are undirected, [0, 1] is the same as [1, 0] and thus will not appear together in edges.
    
## 2. Summary

```text
Union-Find (Connected Components)

init:
    parent[i] = i
    rank[i] = 0
    res = n

find(x):
    if parent[x] != x:
        parent[x] = find(parent[x])
    return parent[x]

union(x, y):
    px, py = find(x), find(y)
    if px == py:
        return 0
    attach smaller rank under larger
    if equal → increase rank
    return 1

for u, v in edges:
    res -= union(u, v)

return res
```

## 3. Key Points

```text
each node starts as its own component (res = n)

find: get root + path compression

union:
    if same root → no change
    else merge → components -= 1

use rank to keep tree shallow

res -= union(u, v)

final res = number of connected components
```

## 4. Code Implementation:


```
class Solution:
    def countComponents(self, n: int, edges):
        parent = list(range(n))
        rank = [0] * n

        def find(x):
            if parent[x] != x:
                parent[x] = find(parent[x])  # path compression
            return parent[x]

        def union(x, y):
            px, py = find(x), find(y)
            if px == py:
                return 0  # no merge
            if rank[px] > rank[py]:
                parent[py] = px
            elif rank[px] < rank[py]:
                parent[px] = py
            else:
                parent[py] = px
                rank[px] += 1
            return 1  # merged

        res = n  # initially n components
        for u, v in edges:
            res -= union(u, v)

        return res   

```

## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note

find: used self.find instead of local find  
rank: used as function rank(px) instead of rank[px]  
union: missing return 1 after merge  
rank compare: inconsistent order (px vs py)  

**Second**

* Used `px > py` to decide union instead of comparing `rank[px]` and `rank[py]`.
* Confused node values with rank in union logic.
* Potentially incorrect tree merging due to wrong union-by-rank implementation.

**Third**

* missing return in `find()` base case
* `find()` may return None → breaks union
* must always return root (`parent[x]`)
* union + rank logic correct
* overall approach correct (Union-Find)

