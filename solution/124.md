## 1. Problem

124. Binary Tree Maximum Path Sum
    
## 2. Summary

DFS

```
1. Initialize global max = -inf
2. DFS(node):
    if not node: return 0
    left = max(0, DFS(node.left))
    right = max(0, DFS(node.right))
    global max = max(global max, node.val + left + right)
    return node.val + max(left, right)
3. Call DFS(root), return global max
```

## 3. Key Points

* Only consider positive contributions for recursion (`max(0, ...)`).
* Update global max including both left + right + node.
* Return node + max(left, right) for recursion.

**Note**

## 1️⃣ Path Shapes

There are two possible path shapes in this problem:

1. **Straight path** (a line extending up from a node)

   * This path can continue to the parent
   * Example: `15 → 20 → parent of 20`
   * **Return value** is the sum of this path
2. **Peak path** (both left and right + current node)

   * This path **cannot continue to the parent**, otherwise nodes would be reused
   * Example: `15 → 20 ← 7`
   * This forms a complete local maximum path
   * **Cannot be returned to parent**, only recorded in the global max

---

## 2️⃣ Return Value vs Global Maximum

| Concept      | Purpose              | Left/Right | Add Node | Can be used by parent? |
| ------------ | -------------------- | ---------- | -------- | ---------------------- |
| Return value | Extend upward        | Only one   | Current  | ✔                      |
| Global max   | Record complete path | Both       | Current  | ✖ (update only)        |

* **Return value selects one side only**: keeps path valid for parent extension
* **Global max updates peak**: records complete peak path, ensures local maximums are captured

💡 **Formula:**

```text
Path to parent = node.val + max(left, right)
Local peak path = node.val + left + right
```

---

## 3️⃣ Why it works

* Every node updates the **global maximum** → all possible paths are considered
* Return value to parent is “extendable contribution” → recursion remains valid
* Final `self.max_sum` holds **maximum path sum in the tree**, whether it passes through root or not

> In other words:
> **Return value is only part of the recursion, not the answer**
> **Answer is recorded in global variable**, covering all peak paths

---

## 4️⃣ Analogy

Imagine climbing a mountain:

* You can walk up a trail (return value)
* But the peak may have a complete mountain with high left and right sides; you take a photo at the peak (global max)
* Parent node can only continue along one trail, cannot carry the whole peak
* The largest photo among all peaks is the final answer




## 4. Code Implementation:

```
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def maxPathSum(self, root: Optional[TreeNode]) -> int:
        self.max_sum = float('-inf')

        def max_gain(node):
            if not node:
                return 0
            
            left_gain = max_gain(node.left)
            left_gain = max(left_gain, 0)

            right_gain = max_gain(node.right)
            right_gain = max(right_gain, 0)

            path_sum = left_gain + right_gain + node.val
            self.max_sum = max(self.max_sum, path_sum)

            return node.val + max(left_gain, right_gain)

        max_gain(root)

        return self.max_sum 
```

## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note

**Second**

Not familiar with the idea. Still need to check the note.

* `vmax` without `self.` → local variable, cannot be updated correctly inside recursion.
* Must use `self.vmax` (or `nonlocal vmax`) to share updates across recursive calls.

**Third**

• Forgot that `return` in Python returns `None` by default.
• DFS is defined to return a numeric path sum, but you risked returning `None`.
• `max(0, left)` requires `left` to be an int, not `None`.
• Violated the rule: recursive functions must always return a consistent type.
• Base case must match the meaning of the function (empty node → path sum = 0).

**Fourth**

* Confused **global result (`self.maxS`)** with **DFS return value**.
* Used `return self.maxS` in recursion instead of returning **contribution to parent**.
* Did not separate **global max path** vs **single-branch path returned upward**.
* Misunderstood that DFS must return **one side only** (`node.val + max(left, right)`) because a parent path cannot take both branches.
* Forgot that **both sides (`left + right + node.val`) are only used to update the global maximum**, not returned upward.
* Base case should return **0 for null node**, not the global variable.


