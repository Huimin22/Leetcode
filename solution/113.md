## 1. Problem

113. Path Sum II
    
## 2. Summary

DFS preorder + path + backtrack

```
res = []

def dfs(node, remain, path):
    if not node: return

    path.append(node.val)

    if not node.left and not node.right:
        if node.val == remain:
            res.append(path[:])
    else:
        dfs(node.left, remain - node.val, path)
        dfs(node.right, remain - node.val, path)

    path.pop()

dfs(root, targetSum, [])
return res
```

## 3. Key Points

DFS preorder traversal
path records current route
backtrack after recursion
check at leaf node
remain - node.val during traversal
append path[:] when valid

## 4. Code Implementation:


```
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def pathSum(self, root: Optional[TreeNode], targetSum: int) -> List[List[int]]:
        res = []

        def dfs(node, remaining, path):
            if not node:
                return

            path.append(node.val)

            if not node.left and not node.right:
                if remaining == node.val:
                    res.append(path[:])
            else:
                dfs(node.left, remaining - node.val, path)
                dfs(node.right, remaining - node.val, path)

            path.pop()  

        dfs(root, targetSum, [])
        return res          

```

## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note

**Second**

Straight dfs. 
Add node to path and check if the node is leaf. 
path.append(node.val)
If it is the leaf, should check the remain numbers == node.val, otherwise it will add wrong path.

**Third**

should check if remaining == node.val:
res.append(path[:])

it has to add node.val to path every time, it is necessary, also do path.pop.


**Fourth**

* **Recursive Short-circuiting**: Used `elif` between left and right child calls, causing the algorithm to skip the right subtree if a left child existed.
* **Subtraction Syntax**: Used `remain--node.val` which Python interprets as `remain - (-node.val)`, effectively performing addition instead of subtraction.
* **Evaluation Timing**: Checked `if remain == 0` before subtracting the current `node.val` from the target, leading to incorrect sum validation at leaf nodes.
* **Missing Base Case**: Initial code lacked a null check (`if not node`), causing crashes on empty trees or unbalanced branches.

**Fourth**

should check if remaining == node.val:

