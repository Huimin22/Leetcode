## 1. Problem

226. Invert Binary Tree
   
## 2. Summary

> “Base → invert children → swap → return root”

## 3. Key Points

* **Base case:** if node is null → return null
* **Recursive step:** invert left subtree, invert right subtree → save results
* **Action:** swap left and right subtrees
* **Return:** current node

## 4. Code Implementation:

```
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def invertTree(self, root: Optional[TreeNode]) -> Optional[TreeNode]:
        if not root:
            return None
        left = self.invertTree(root.left)
        right = self.invertTree(root.right)
        root.left = right
        root.right = left
        return root   
```

## 5. Complexity Analysis
    Time：O(n)
        
    Space: O(1)
        
## 6. Edge Cases

## 7. Follow-ups

## 8. Note

* Base case wrong: returned `[]` instead of `None` → breaks tree structure.
* Forgot to save left/right results before swapping → risk losing original subtrees.
* Recursive call inside class must use `self.` → otherwise function not found.
* Confused LeetCode `[]` input/output with Python `None` → inside function always use `None`.


